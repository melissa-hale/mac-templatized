freshness:
  - table: "{{ table }}" # raw:normalized_events.bi OR rentaldata:dev.fct_reviews_cleansed
    name: SEV0 Monitoring hourly freshness
    freshness_threshold: 120
    description: Default SEV0 freshness rule for hourly freshness
    schedule:
      type: fixed  # must be fixed
      start_time: "{{ startTime }}"
      interval_minutes: 60
    labels:
      - sev1
      - freshness
      - snowflake2
    severity: sev0